# nai-v5 集成说明（供仓库维护者使用）

说明：我在 feat/nai-v5 分支新增了一个配置文件 `config/nai_v5.json`，其中包含了 NovelAI 新版模型 nai-v5 的占位配置和 UI 控件规范。为了最小范围地改动现有 userscript（保持格式并不破坏原有逻辑），推荐以下两种集成方式：

方法 A — 自动/直接修改 userscript（建议，经你授权我可代为提交）：
1. 在文件 `酒馆助手脚本-生图助手-本地自改版-0807-1.txt` 中找到一行：

   "data": {}

2. 将其替换为（整段替换，注意保持 JSON 格式和缩进）:

```json
"data": <请将整个 `config/nai_v5.json` 的内容粘贴到此处替换>
```

3. 在用户面板生成/初始化代码部分（通常会在 userscript 的 `content` 字符串里，搜索 `panel` / `模型` / `model` 字样），将模型下拉的选项追加 `nai-v5`，并将面板控件按照 `config/nai_v5.json` -> `ui.panel_controls` 的顺序绑定到请求参数。

4. 在调用模型的地方（发送到后端的请求 payload 中），当选中 `nai-v5` 时，将 model id 字段填为 `nai/v5`（或你提供的真实模型 id/权重 URL）；如果需要额外的 v5 高级参数，使用 `enable_v5_advanced` 去切换对应的参数集。

方法 B — 不改 userscript，仅放置配置（我已实现，当前分支）：
- 我已经创建了 `config/nai_v5.json`（占位配置）和本说明文档。如果你希望自己在本地手动合并：
  - 打开 userscript 文本（`酒馆助手脚本-生图助手-本地自改版-0807-1.txt`）
  - 按方法 A 第 2 步替换 `"data": {}` 段为 `config/nai_v5.json` 内容
  - 手动在面板逻辑中追加 `nai-v5` 到模型下拉并绑定参数

替换占位模型 id：
- 我使用了占位符 `nai/v5`。如果你有正式模型 id 或权重 URL（例如 NovelAI 的 registry 地址或 ckpt 链接），请替换 `nai/v5` 为该值。

提交与后续：
- 我已把这两个文件提交到分支 `feat/nai-v5`。如果你授权我继续直接修改 userscript，我可以在该分支上把 `酒馆助手脚本-生图助手-本地自改版-0807-1.txt` 中的 `"data": {}` 段替换为配置并追加模型下拉项，然后提交最终变更并把整个变更以 JSON 返回给你。

注：我将严格遵守“不得改动其它东西”的要求——仅对必要的 `data` 配置段和面板相关小段进行增量修改，并保留原文件的其余内容与格式。
